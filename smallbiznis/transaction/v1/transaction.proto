syntax = "proto3";

package smallbiznis.transaction.v1;

option go_package = "github.com/smallbiznis/smallbiznisapis/smallbiznis/transaction/v1;transactionv1";

import "google/protobuf/timestamp.proto";
import "google/protobuf/struct.proto";
import "google/api/annotations.proto";

// Internal Common
import "smallbiznis/common/money.proto";

// ========================================================
// ENUMS
// ========================================================

enum TransactionStatus {
  TRANSACTION_STATUS_UNSPECIFIED = 0;
  TRANSACTION_STATUS_PENDING = 1;
  TRANSACTION_STATUS_SUCCESS = 2;
  TRANSACTION_STATUS_FAILED = 3;
  TRANSACTION_STATUS_CANCELLED = 4;
  TRANSACTION_STATUS_REFUNDED = 5;
}

enum PaymentMethod {
  PAYMENT_METHOD_UNSPECIFIED = 0;
  PAYMENT_METHOD_CASH = 1;
  PAYMENT_METHOD_CARD = 2;
  PAYMENT_METHOD_QRIS = 3;
  PAYMENT_METHOD_TRANSFER = 4;
  PAYMENT_METHOD_WALLET = 5;
}

enum ChannelType {
  CHANNEL_TYPE_UNSPECIFIED = 0;
  CHANNEL_TYPE_ONLINE = 1;
  CHANNEL_TYPE_POS = 2;
  CHANNEL_TYPE_API = 3;
}

// ========================================================
// MESSAGES
// ========================================================

message TransactionItem {
  string item_id = 1;                          // optional: return server-generated ID
  string transaction_id = 2;                   // optional: set by server
  string tenant_id = 3;                        // optional: set by server

  string sku = 4;
  string name = 5;
  string category = 6;

  int32 qty = 7;
  smallbiznis.common.Money price = 8;      // per unit (minor units)
  smallbiznis.common.Money total = 9;      // qty * price (server may compute)

  google.protobuf.Struct metadata = 10;        // e.g. variants, notes
  google.protobuf.Timestamp created_at = 11;   // server-populated
  google.protobuf.Timestamp updated_at = 12;   // server-populated
}

// Transaction entity
message Transaction {
  string id = 1;
  string tenant_id = 2;
  string user_id = 3;

  // Optional external reference, e.g., POS or e-commerce order ID
  string order_id = 4;

  smallbiznis.common.Money amount = 5;

  TransactionStatus status = 6;
  PaymentMethod payment_method = 7;
  ChannelType channel = 8;
  google.protobuf.Struct metadata = 9;

  google.protobuf.Timestamp created_at = 10;
  google.protobuf.Timestamp updated_at = 11;
  string transaction_id = 12;
  repeated TransactionItem items = 13;
}

// ========================================================
// REQUESTS / RESPONSES
// ========================================================

message CreateTransactionRequest {
  string tenant_id = 1;
  string user_id = 2;
  string order_id = 3;
  smallbiznis.common.Money amount = 4;
  PaymentMethod payment_method = 5;
  google.protobuf.Struct metadata = 6;
  repeated TransactionItem items = 7;
}

message CreateTransactionResponse {
  Transaction transaction = 1;
}

message GetTransactionRequest {
  string tenant_id = 1;
  string transaction_id = 2;
}

message GetTransactionResponse {
  Transaction transaction = 1;
}

message ListTransactionsRequest {
  string tenant_id = 1;
  int32 limit = 2;
  string cursor = 3;
  TransactionStatus filter_status = 4;
}

message ListTransactionsResponse {
  repeated Transaction transactions = 1;
  string next_cursor = 2;
}

message UpdateTransactionStatusRequest {
  string tenant_id = 1;
  string transaction_id = 2;
  TransactionStatus status = 3;
}

message UpdateTransactionStatusResponse {
  Transaction transaction = 1;
}

// ========================================================
// SERVICE DEFINITION
// ========================================================

service TransactionService {
  // Create a new transaction
  rpc CreateTransaction(CreateTransactionRequest) returns (CreateTransactionResponse) {
    option (google.api.http) = {
      post: "/v1/transactions"
      body: "*"
    };
  }

  // Get a single transaction
  rpc GetTransaction(GetTransactionRequest) returns (GetTransactionResponse) {
    option (google.api.http) = {
      get: "/v1/transactions/{transaction_id}"
    };
  }

  // List transactions (paginated)
  rpc ListTransactions(ListTransactionsRequest) returns (ListTransactionsResponse) {
    option (google.api.http) = {
      get: "/v1/transactions"
    };
  }

  // Update status (used for marking success/failed/refunded)
  rpc UpdateTransactionStatus(UpdateTransactionStatusRequest) returns (UpdateTransactionStatusResponse) {
    option (google.api.http) = {
      patch: "/v1/transactions/{transaction_id}/status"
      body: "*"
    };
  }
}

// ========================================================
// EVENT SCHEMA
// ========================================================

// Emitted to message broker (Kafka/NATS/Asynq) when transaction completes or updates.
message TransactionEvent {
  string tenant_id = 1;
  string transaction_id = 2;
  string user_id = 3;
  smallbiznis.common.Money amount = 4;
  ChannelType channel = 5;
  TransactionStatus status = 6;
  google.protobuf.Struct metadata = 7;
  google.protobuf.Timestamp created_at = 8;
}
